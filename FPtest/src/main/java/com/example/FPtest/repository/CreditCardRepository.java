/*
package com.example.FPtest.repository;

import com.example.FPtest.model.CreditCard;
import com.example.FPtest.model.User;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.math.BigInteger;
import java.util.List;
import java.util.Optional;

@Repository
public interface CreditCardRepository extends JpaRepository<CreditCard, BigInteger> {
    Optional<CreditCard> findByUsername(String username);
    List<CreditCard> findByUser(User user);

    List <CreditCard> findByCardNumber(BigInteger cardNumber);

}

 */



